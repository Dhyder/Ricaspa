const $=s=>document.querySelector(s),state={user:null,section:'overview'};
async function api(path,options={}){const r=await fetch(path,{credentials:'same-origin',headers:{'Content-Type':'application/json',...(options.headers||{})},...options});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||`Request failed (${r.status})`);return d}
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const money=v=>v==null||v===''?'—':`KES ${Number(v).toLocaleString()}`;
const scalar=(v,fallback=0)=>{if(v&&typeof v==='object')return Number(v.totalBookings??v.totalOrders??v.total??v.count??0);return Number(v??fallback)};
const date=v=>v?new Date(v).toLocaleString(): '—';
function shell(){document.body.innerHTML=`<div class="app"><aside class="sidebar"><div class="brand"><img src="/assets/img/logo.png" alt="Rica Spa"><div><strong>Rica Spa</strong><small>Admin workspace</small></div></div><div class="nav-label">Workspace</div><nav id="nav"><button data-section="overview">Overview</button><button data-section="bookings">Bookings</button><button data-section="vouchers">Vouchers</button><button data-section="orders">Orders</button></nav><div class="nav-label superuser-only">Administration</div><nav id="admin-nav" class="superuser-only"><button data-section="users">Staff & Users</button><button data-section="audit">Activity Log</button><button data-section="profile">My Profile</button></nav><div class="side-foot"><div><span id="who"></span><small id="role"></small></div><button id="logout">Sign out</button></div></aside><main><header><div><p class="eyebrow">Rica Spa / Admin</p><h1 id="title">Overview</h1><p id="subtitle" class="subtitle"></p></div><button class="profile-badge" id="profile-badge" data-section="profile" title="Open profile"></button></header><section id="content"></section></main></div>`;document.querySelector('.sidebar').onclick=e=>{const b=e.target.closest('button[data-section]');if(b){e.preventDefault();state.section=b.dataset.section;render()}};document.querySelectorAll('.superuser-only').forEach(n=>{if(state.user.role!=='superuser')n.remove()});$('#logout').onclick=async()=>{try{await api('/api/dashboard-logout',{method:'POST'})}finally{location.href='/dashboard/login.html'}}}
async function render(){const meta={overview:['Overview',"A calm, clear view of today's work."],bookings:['Bookings','Review requests, confirm appointments and keep the day moving.'],vouchers:['Voucher Desk','See issued vouchers, customer details, value and redemption status.'],orders:['Orders','Monitor voucher purchases and payment status.'],users:['Staff & Users','Manage who has access to the Rica operations desk.'],audit:['Activity Log','See who approved, redeemed or changed what.'],profile:['My Profile','Your account, access level and recent operational identity.']}[state.section];if(!meta)return;$('#title').textContent=meta[0];$('#subtitle').textContent=meta[1];document.querySelectorAll('[data-section]').forEach(b=>b.classList.toggle('active',b.dataset.section===state.section));$('#content').innerHTML='<div class="panel"><div class="empty">Loading…</div></div>';try{if(state.section==='overview')await overview();else if(state.section==='bookings')await bookings();else if(state.section==='vouchers')await vouchers();else if(state.section==='orders')await orders();else if(state.section==='users')await users();else if(state.section==='audit')await audit();else await profile()}catch(e){$('#content').innerHTML=`<div class="notice error"><strong>Unable to load this workspace</strong><p>${esc(e.message)}</p><button class="secondary" onclick="render()">Try again</button></div>`}}
async function overview(){const d=await api('/api/dashboard-stats');const bookings=scalar(d.totalBookings??d.bookings),pending=scalar(d.pendingBookings??d.pending),orders=scalar(d.totalOrders??d.orders),revenue=scalar(d.revenue??d.totalRevenue);$('#content').innerHTML=`<div class="welcome"><div><span class="section-kicker">Welcome back</span><h2>${esc(state.user.name)}</h2><p>Your Rica operations workspace is ready. Every approval and redemption is attributed to your account.</p></div><div class="welcome-mark">R</div></div><div class="cards"><article class="card"><span>Total bookings</span><strong>${bookings}</strong><small>Booking requests in the system</small></article><article class="card"><span>Pending</span><strong>${pending}</strong><small>Requests waiting for attention</small></article><article class="card"><span>Orders</span><strong>${orders}</strong><small>Voucher orders recorded</small></article><article class="card"><span>Revenue</span><strong>${money(revenue)}</strong><small>Recorded voucher revenue</small></article></div><div class="quick-grid"><button class="quick" data-section="bookings"><span>01</span><b>Review bookings</b><small>Open upcoming and pending requests</small></button><button class="quick" data-section="vouchers"><span>02</span><b>Voucher desk</b><small>View customer, value and redemption status</small></button><button class="quick" data-section="users"><span>03</span><b>Team access</b><small>Manage staff and account status</small></button></div>`}
async function bookings(){const d=await api('/api/dashboard-bookings?upcoming=1'),rows=d.bookings||d.results||[];$('#content').innerHTML=`<div class="panel"><div class="panel-head"><div><span class="section-kicker">Appointments</span><h2>Booking requests</h2></div><button class="secondary" onclick="render()">Refresh</button></div><div class="table-wrap"><table><thead><tr><th>Guest</th><th>Service</th><th>Date</th><th>Time</th><th>Status</th><th>Update</th></tr></thead><tbody>${rows.map(r=>`<tr><td><strong>${esc(r.name||r.full_name||'Guest')}</strong><small>${esc(r.email||r.phone||'')}</small></td><td>${esc(r.service||'—')}</td><td>${esc(r.preferred_date||r.date||'—')}</td><td>${esc(r.preferred_time||r.time||'—')}</td><td><span class="pill">${esc(r.status||'new')}</span></td><td><select onchange="bookingStatus('${esc(r.ref)}',this.value)"><option value="">Choose status…</option><option value="confirmed">Confirmed</option><option value="declined">Declined</option><option value="completed">Completed</option><option value="no-show">No-show</option></select></td></tr>`).join('')||'<tr><td colspan="6" class="empty">No upcoming booking requests found.</td></tr>'}</tbody></table></div></div>`}
window.bookingStatus=async(ref,status)=>{if(!status)return;try{await api('/api/update-booking-status',{method:'POST',body:JSON.stringify({ref,status})});await render()}catch(e){alert(e.message)}};
async function orders(){const d=await api('/api/dashboard-orders'),rows=d.orders||d.results||[];$('#content').innerHTML=`<div class="panel"><div class="panel-head"><div><span class="section-kicker">Sales</span><h2>Voucher orders</h2><p class="subtitle">Customer and payment state from the durable order ledger.</p></div><button class="secondary" onclick="render()">Refresh</button></div><div class="table-wrap"><table><thead><tr><th>Reference</th><th>Customer</th><th>Service</th><th>Amount</th><th>Payment</th><th>Fulfilment</th><th>Created</th></tr></thead><tbody>${rows.map(r=>`<tr><td><strong>${esc(r.ref||r.reference||'—')}</strong><small>${esc(r.voucher_code||'')}</small></td><td><strong>${esc(r.buyer_name||r.name||'—')}</strong><small>${esc(r.buyer_email||r.email||r.buyer_phone||'')}</small></td><td>${esc(r.service_name||r.service||r.type||'Voucher')}</td><td>${money(r.value??r.amount)}</td><td><span class="pill">${esc(r.payment_state||r.status||'—')}</span></td><td><span class="pill">${esc(r.finalization_state||'—')}</span></td><td>${date(r.created_at)}</td></tr>`).join('')||'<tr><td colspan="7" class="empty">No orders found.</td></tr>'}</tbody></table></div></div>`}
async function vouchers(){const d=await api('/api/dashboard-vouchers'),rows=d.vouchers||[];$('#content').innerHTML=`<div class="panel"><div class="panel-head"><div><span class="section-kicker">Voucher ledger</span><h2>Issued vouchers</h2><p class="subtitle">Customer, value and redemption status from the VOUCHERS store.</p></div><button class="secondary" onclick="render()">Refresh</button></div><div class="table-wrap"><table><thead><tr><th>Reference</th><th>Customer</th><th>Service</th><th>Value</th><th>Status</th><th>Created</th><th>Expiry</th><th>Redeemed</th></tr></thead><tbody>${rows.map(v=>`<tr><td><strong>${esc(v.ref||v.code||'—')}</strong><small>${esc(v.code||'')}</small></td><td><strong>${esc(v.buyerName||'—')}</strong><small>${esc(v.buyerEmail||v.buyerPhone||'')}</small></td><td>${esc(v.serviceName||v.type||'—')}</td><td>${money(v.value)}</td><td><span class="pill">${esc(v.status||'unredeemed')}</span></td><td>${date(v.createdAt)}</td><td>${date(v.expiresAt)}</td><td>${v.redeemedAt?`${date(v.redeemedAt)}<small>${esc(v.redeemedBy||'')}</small>`:'—'}</td></tr>`).join('')||'<tr><td colspan="8" class="empty">No vouchers found.</td></tr>'}</tbody></table></div></div>`}
async function users(){if(state.user.role!=='superuser')throw Error('Superuser access required.');const d=await api('/api/dashboard-users'),rows=d.users||[];$('#content').innerHTML=`<div class="grid-two"><div class="panel form-panel"><span class="section-kicker">Administration</span><h2>Add staff member</h2><p class="form-intro">Create an individual account so every operational action has an owner.</p><form id="user-form"><label>Full name<input name="name" placeholder="e.g. Jane Wanjiku" required></label><label>Email address<input name="email" type="email" placeholder="staff@ricaspa.co.ke" required></label><label>Temporary password<input name="password" type="password" minlength="8" placeholder="At least 8 characters" required></label><label>Access level<select name="role"><option value="employee">Staff</option><option value="superuser">Superuser</option></select></label><button>Create staff account</button></form></div><div class="panel"><div class="panel-head"><div><span class="section-kicker">Access</span><h2>People with access</h2></div><button class="secondary" onclick="users()">Refresh</button></div><div class="user-list">${rows.map(u=>`<div class="user-row"><div class="avatar">${esc((u.name||'?').slice(0,1).toUpperCase())}</div><div class="user-main"><b>${esc(u.name)}</b><small>${esc(u.email)} · Last login: ${esc(date(u.last_login_at))}</small></div><span class="pill">${esc(u.role==='employee'?'staff':u.role)}</span><select onchange="userStatus('${esc(u.id)}',this.value)"><option value="${esc(u.status)}">${esc(u.status)}</option><option value="active">Active</option><option value="disabled">Disabled</option><option value="pending">Pending</option></select></div>`).join('')||'<div class="empty">No staff accounts yet.</div>'}</div></div></div>`;$('#user-form').onsubmit=async e=>{e.preventDefault();const o=Object.fromEntries(new FormData(e.target));const btn=e.target.querySelector('button');btn.disabled=true;try{await api('/api/dashboard-users',{method:'POST',body:JSON.stringify(o)});await users()}catch(x){alert(x.message)}finally{btn.disabled=false}}}
window.userStatus=async(id,status)=>{try{await api('/api/dashboard-users',{method:'PATCH',body:JSON.stringify({id,status})});await users()}catch(e){alert(e.message)}};
async function audit(){if(state.user.role!=='superuser')throw Error('Superuser access required.');const d=await api('/api/dashboard-audit'),rows=d.events||[];$('#content').innerHTML=`<div class="panel"><div class="panel-head"><div><span class="section-kicker">Accountability</span><h2>Activity log</h2><p class="subtitle">Administrative actions are attributed to the staff account that performed them.</p></div><button class="secondary" onclick="render()">Refresh</button></div><div class="table-wrap"><table><thead><tr><th>When</th><th>Staff member</th><th>Action</th><th>Entity</th><th>Details</th></tr></thead><tbody>${rows.map(a=>`<tr><td>${date(a.created_at)}</td><td><strong>${esc(a.user_name||'System')}</strong><small>${esc(a.user_email||'')}</small></td><td><span class="pill">${esc(a.action)}</span></td><td>${esc(a.entity_type||'')} ${esc(a.entity_id||'')}</td><td>${esc(a.metadata||'')}</td></tr>`).join('')||'<tr><td colspan="5" class="empty">No activity recorded yet.</td></tr>'}</tbody></table></div></div>`}
async function profile(){$('#content').innerHTML=`<div class="profile-grid"><div class="panel profile-hero"><div class="profile-avatar">${esc((state.user.name||'R').slice(0,1).toUpperCase())}</div><span class="section-kicker">Account</span><h2>${esc(state.user.name)}</h2><p>${esc(state.user.email)}</p><span class="pill">${esc(state.user.role==='employee'?'Staff':state.user.role)}</span></div><div class="panel"><span class="section-kicker">Identity</span><h2>Operational profile</h2><dl class="details"><div><dt>Name</dt><dd>${esc(state.user.name)}</dd></div><div><dt>Email</dt><dd>${esc(state.user.email)}</dd></div><div><dt>Access</dt><dd>${esc(state.user.role==='employee'?'Staff':'Superuser')}</dd></div><div><dt>Session</dt><dd>Authenticated</dd></div></dl></div></div>`}
(async()=>{try{const d=await api('/api/dashboard-me');state.user=d.user;shell();$('#who').textContent=state.user.name;$('#role').textContent=state.user.role==='superuser'?'Superuser':'Staff';$('#profile-badge').textContent=(state.user.name||'R').slice(0,1).toUpperCase();await render()}catch(e){location.href='/dashboard/login.html'}})();