const $=s=>document.querySelector(s),S={user:null,section:'overview'};async function api(p,o={}){const r=await fetch(p,{credentials:'same-origin',headers:{'Content-Type':'application/json',...(o.headers||{})},...o}),d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||`Request failed (${r.status})`);return d}const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const money=v=>v==null?'—':`KES ${Number(v).toLocaleString()}`;
function shell(){document.body.innerHTML=`<div class="app"><aside class="sidebar"><div class="brand"><img src="/assets/img/logo.png" alt="Rica Spa"><span>Rica Spa</span></div><nav id="nav"><button data-section="overview">Overview</button><button data-section="bookings">Bookings</button><button data-section="vouchers">Vouchers</button><button data-section="orders">Orders</button><button data-section="users" class="superuser-only">Staff & Users</button><button data-section="audit" class="superuser-only">Activity</button></nav><div class="side-foot"><span id="role"></span><button id="logout">Sign out</button></div></aside><main><header><div><p class="eyebrow">Rica Spa operations</p><h1 id="title">Overview</h1></div><div class="profile" id="who"></div></header><section id="content"></section></main></div>`;$('#nav').onclick=e=>{const b=e.target.closest('button[data-section]');if(b){S.section=b.dataset.section;render()}};$('#logout').onclick=async()=>{await api('/api/dashboard-logout',{method:'POST'});location.href='/dashboard/login.html'}}
async function render(){const titles={overview:'Overview',bookings:'Bookings',vouchers:'Voucher Desk',orders:'Orders',users:'Staff & Users',audit:'Activity Log'};$('#title').textContent=titles[S.section];document.querySelectorAll('#nav button').forEach(b=>b.classList.toggle('active',b.dataset.section===S.section));try{return ({overview,bookings,vouchers,orders,users,audit})[S.section]()}catch(e){$('#content').innerHTML=`<div class="notice error">${esc(e.message)}</div>`}}
async function overview(){const d=await api('/api/dashboard-stats');$('#content').innerHTML=`<div class="cards">${[['Bookings',d.bookings??d.totalBookings??0],['Pending',d.pending??d.pendingBookings??0],['Orders',d.orders??d.totalOrders??0],['Revenue',money(d.revenue??d.totalRevenue??0)]].map(x=>`<article class="card"><span>${x[0]}</span><strong>${x[1]}</strong></article>`).join('')}</div><div class="panel"><h2>Welcome back, ${esc(S.user.name)}</h2><p>Signed in as <b>${esc(S.user.role)}</b>. Approvals, redemptions and administration are attributed to your account.</p></div>`}
async function bookings(){const d=await api('/api/dashboard-bookings'),rows=d.bookings||d.results||[];$('#content').innerHTML=`<div class="panel"><div class="panel-head"><h2>Booking requests</h2><button class="secondary" onclick="render()">Refresh</button></div><div class="table-wrap"><table><thead><tr><th>Guest</th><th>Service</th><th>Date</th><th>Time</th><th>Status</th><th>Action</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${esc(r.name||r.full_name)}</td><td>${esc(r.service)}</td><td>${esc(r.date)}</td><td>${esc(r.time)}</td><td><span class="pill">${esc(r.status)}</span></td><td><select onchange="bookingStatus('${esc(r.ref)}',this.value)"><option value="">Update…</option><option>confirmed</option><option>declined</option><option>completed</option><option>no-show</option></select></td></tr>`).join('')||'<tr><td colspan="6">No bookings found.</td></tr>'}</tbody></table></div></div>`}window.bookingStatus=async(ref,status)=>{if(!status)return;try{await api('/api/update-booking-status',{method:'POST',body:JSON.stringify({ref,status})});await render()}catch(e){alert(e.message)}};
async function orders(){const d=await api('/api/dashboard-orders'),rows=d.orders||d.results||[];$('#content').innerHTML=`<div class="panel"><h2>Voucher orders</h2><div class="table-wrap"><table><thead><tr><th>Reference</th><th>Customer</th><th>Amount</th><th>Status</th><th>Created</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${esc(r.ref||r.reference)}</td><td>${esc(r.email||r.customer_email)}</td><td>${money(r.amount)}</td><td><span class="pill">${esc(r.status)}</span></td><td>${esc(r.created_at)}</td></tr>`).join('')||'<tr><td colspan="5">No orders found.</td></tr>'}</tbody></table></div></div>`}
function vouchers(){$('#content').innerHTML=`<div class="grid-two"><div class="panel"><h2>Look up voucher</h2><form id="vf"><label>Voucher code<input name="code" placeholder="RICA-XXXX-XXXX" required></label><button>Look up</button></form><div id="vr"></div></div><div class="panel"><h2>Redeem</h2><p>Verify the code with the guest, then redeem it. The action is recorded against your staff account.</p><button id="redeem" class="danger" disabled>Redeem voucher</button></div></div>`;let cur=null;$('#vf').onsubmit=async e=>{e.preventDefault();try{const code=new FormData(e.target).get('code'),d=await api('/api/redeem-voucher',{method:'POST',body:JSON.stringify({code,action:'lookup'})});cur=d.voucher;$('#vr').innerHTML=`<div class="voucher"><b>${esc(cur.code||code)}</b><span>Status: ${esc(cur.status)}</span><span>Service: ${esc(cur.service||cur.serviceName||'Voucher')}</span><span>Value: ${money(cur.amount||cur.value)}</span></div>`;$('#redeem').disabled=cur.status==='redeemed'}catch(x){$('#vr').innerHTML=`<div class="notice error">${esc(x.message)}</div>`}};$('#redeem').onclick=async()=>{if(!cur)return;try{await api('/api/redeem-voucher',{method:'POST',body:JSON.stringify({code:cur.code,action:'redeem'})});alert('Voucher redeemed.');cur=null;$('#redeem').disabled=true}catch(x){alert(x.message)}}}
async function users(){if(S.user.role!=='superuser')return $('#content').innerHTML='<div class="notice error">Superuser access required.</div>';const d=await api('/api/dashboard-users');$('#content').innerHTML=`<div class="grid-two"><div class="panel"><h2>Add staff</h2><form id="uf"><label>Name<input name="name" required></label><label>Email<input name="email" type="email" required></label><label>Password<input name="password" type="password" minlength="8" required></label><label>Role<select name="role"><option value="employee">Staff</option><option value="superuser">Superuser</option></select></label><button>Create account</button></form></div><div class="panel"><h2>People with access</h2><div class="user-list">${(d.users||[]).map(u=>`<div class="user-row"><div><b>${esc(u.name)}</b><small>${esc(u.email)}</small></div><div><span class="pill">${esc(u.role)}</span><select onchange="userStatus('${esc(u.id)}',this.value)"><option>${esc(u.status)}</option><option>active</option><option>disabled</option><option>pending</option></select></div></div>`).join('')}</div></div></div>`;$('#uf').onsubmit=async e=>{e.preventDefault();try{await api('/api/dashboard-users',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});await users()}catch(x){alert(x.message)}}}window.userStatus=async(id,status)=>{try{await api('/api/dashboard-users',{method:'PATCH',body:JSON.stringify({id,status})});await users()}catch(e){alert(e.message)}};
async function audit(){const d=await api('/api/dashboard-audit');$('#content').innerHTML=`<div class="panel"><div class="panel-head"><h2>Who did what</h2><button class="secondary" onclick="render()">Refresh</button></div><div class="table-wrap"><table><thead><tr><th>When</th><th>Staff member</th><th>Action</th><th>Entity</th></tr></thead><tbody>${(d.events||[]).map(a=>`<tr><td>${esc(a.created_at)}</td><td>${esc(a.user_name||'System')}</td><td>${esc(a.action)}</td><td>${esc(a.entity_type||'')} ${esc(a.entity_id||'')}</td></tr>`).join('')||'<tr><td colspan="4">No activity yet.</td></tr>'}</tbody></table></div></div>`}
(async()=>{try{S.user=(await api('/api/dashboard-me')).user;shell();$('#who').textContent=S.user.name;$('#role').textContent=S.user.role==='superuser'?'Superuser':'Staff';if(S.user.role!=='superuser')document.querySelectorAll('.superuser-only').forEach(x=>x.remove());await render()}catch{location.href='/dashboard/login.html'}})();
