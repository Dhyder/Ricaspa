const $=s=>document.querySelector(s),state={user:null,section:'overview'};async function api(path,options={}){const r=await fetch(path,{credentials:'same-origin',headers:{'Content-Type':'application/json',...(options.headers||{})},...options});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||`Request failed (${r.status})`);return d}const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const money=v=>v==null?'—':`KES ${Number(v).toLocaleString()}`;function shell(){document.body.innerHTML=`<div class="app"><aside class="sidebar"><div class="brand"><img src="/assets/img/logo.png" alt="Rica Spa"><div><strong>Rica Spa</strong><small>Operations</small></div></div><div class="nav-label">Workspace</div><nav id="nav"><button data-section="overview">Overview</button><button data-section="bookings">Bookings</button><button data-section="vouchers">Vouchers</button><button data-section="orders">Orders</button></nav><div class="nav-label admin-label superuser-only">Administration</div><nav class="superuser-only"><button data-section="users">Staff & Users</button><button data-section="audit">Activity Log</button></nav><div class="side-foot"><div><span id="who"></span><small id="role"></small></div><button id="logout">Sign out</button></div></aside><main><header><div><p class="eyebrow">Rica Spa operations</p><h1 id="title">Overview</h1><p id="subtitle" class="subtitle">A calm, clear view of today's work.</p></div><div class="profile-badge" id="profile-badge"></div></header><section id="content"></section></main></div>`;$('#nav').onclick=e=>{const b=e.target.closest('button[data-section]');if(b){state.section=b.dataset.section;render()}};$('#logout').onclick=async()=>{await api('/api/dashboard-logout',{method:'POST'});location.href='/dashboard/login.html'}}async function render(){const meta={overview:['Overview',"A calm, clear view of today's work."],bookings:['Bookings','Review requests, confirm appointments and keep the day moving.'],vouchers:['Voucher Desk','Look up and securely redeem Rica Spa vouchers.'],orders:['Orders','Monitor voucher purchases and payment status.'],users:['Staff & Users','Manage who has access to the Rica operations desk.'],audit:['Activity Log','See who approved, redeemed or changed what.']}[state.section];$('#title').textContent=meta[0];$('#subtitle').textContent=meta[1];document.querySelectorAll('[data-section]').forEach(b=>b.classList.toggle('active',b.dataset.section===state.section));try{if(state.section==='overview')return overview();if(state.section==='bookings')return bookings();if(state.section==='vouchers')return vouchers();if(state.section==='orders')return orders();if(state.section==='users')return users();return audit()}catch(e){$('#content').innerHTML=`<div class="notice error"><strong>Something went wrong</strong><p>${esc(e.message)}</p></div>`}}async function overview(){const d=await api('/api/dashboard-stats');$('#content').innerHTML=`<div class="welcome"><div><span class="section-kicker">Welcome back</span><h2>${esc(state.user.name)}</h2><p>Your staff workspace is ready. Actions are attributed to your account automatically.</p></div><div class="welcome-mark">R</div></div><div class="cards">${[['Bookings',d.bookings??d.totalBookings??0,'Today’s requests'],['Pending',d.pending??d.pendingBookings??0,'Need attention'],['Orders',d.orders??d.totalOrders??0,'Voucher orders'],['Revenue',money(d.revenue??d.totalRevenue??0),'Recorded revenue']].map(x=>`<article class="stat-card"><span>${x[0]}</span><strong>${x[1]}</strong><small>${x[2]}</small></article>`).join('')}</div><div class="quick-grid"><button class="quick" onclick="state.section='bookings';render()"><span>01</span><b>Review bookings</b><small>Confirm or update today's requests</small></button><button class="quick" onclick="state.section='vouchers';render()"><span>02</span><b>Voucher desk</b><small>Find and redeem a customer voucher</small></button></div>`}async function bookings(){const d=await api('/api/dashboard-bookings'),rows=d.bookings||d.results||[];$('#content').innerHTML=`<div class="panel"><div class="panel-head"><div><span class="section-kicker">Appointments</span><h2>Booking requests</h2></div><button class="secondary" onclick="render()">Refresh</button></div><div class="table-wrap"><table><thead><tr><th>Guest</th><th>Service</th><th>Date</th><th>Time</th><th>Status</th><th>Update</th></tr></thead><tbody>${rows.map(r=>`<tr><td><strong>${esc(r.name||r.full_name||'Guest')}</strong><small>${esc(r.email||'')}</small></td><td>${esc(r.service||'—')}</td><td>${esc(r.date||'—')}</td><td>${esc(r.time||'—')}</td><td><span class="pill">${esc(r.status||'pending')}</span></td><td><select onchange="bookingStatus('${esc(r.ref)}',this.value)"><option value="">Choose status…</option><option value="confirmed">Confirmed</option><option value="declined">Declined</option><option value="completed">Completed</option><option value="no-show">No-show</option></select></td></tr>`).join('')||'<tr><td colspan="6" class="empty">No booking requests found.</td></tr>'}</tbody></table></div></div>`}window.bookingStatus=async(ref,status)=>{if(!status)return;try{await api('/api/update-booking-status',{method:'POST',body:JSON.stringify({ref,status})});await render()}catch(e){alert(e.message)}};async function orders(){const d=await api('/api/dashboard-orders'),rows=d.orders||d.results||[];$('#content').innerHTML=`<div class="panel"><div class="panel-head"><div><span class="section-kicker">Sales</span><h2>Voucher orders</h2></div><button class="secondary" onclick="render()">Refresh</button></div><div class="table-wrap"><table><thead><tr><th>Reference</th><th>Customer</th><th>Amount</th><th>Status</th><th>Created</th></tr></thead><tbody>${rows.map(r=>`<tr><td><strong>${esc(r.ref||r.reference)}</strong></td><td>${esc(r.email||r.customer_email)}</td><td>${money(r.amount)}</td><td><span class="pill">${esc(r.status)}</span></td><td>${esc(r.created_at)}</td></tr>`).join('')||'<tr><td colspan="5" class="empty">No orders found.</td></tr>'}</tbody></table></div></div>`}function vouchers(){$('#content').innerHTML=`<div class="grid-two"><div class="panel form-panel"><span class="section-kicker">Voucher desk</span><h2>Find a voucher</h2><p class="form-intro">Enter the customer's code to verify its value and status before redemption.</p><form id="voucher-form"><label>Voucher code<span class="hint">Usually begins with RICA-</span><input name="code" placeholder="RICA-XXXX-XXXX" autocomplete="off" required></label><button>Check voucher</button></form><div id="voucher-result"></div></div><div class="panel form-panel"><span class="section-kicker">Secure action</span><h2>Redeem voucher</h2><div class="action-box"><div class="action-icon">✓</div><div><b>Staff attribution enabled</b><p>Once redeemed, the action is recorded against your Rica staff account.</p></div></div><button id="redeem" class="danger full" disabled>Redeem voucher</button></div></div>`;let current=null;$('#voucher-form').onsubmit=async e=>{e.preventDefault();const btn=e.target.querySelector('button');btn.disabled=true;try{const code=new FormData(e.target).get('code'),d=await api('/api/redeem-voucher',{method:'POST',body:JSON.stringify({code,action:'lookup'})});current=d.voucher;$('#voucher-result').innerHTML=`<div class="voucher"><div><span class="voucher-code">${esc(current.code||code)}</span><span class="pill">${esc(current.status)}</span></div><span>Service <b>${esc(current.service||current.serviceName||'Voucher')}</b></span><span>Value <b>${money(current.amount||current.value)}</b></span></div>`;$('#redeem').disabled=current.status==='redeemed'}catch(x){$('#voucher-result').innerHTML=`<div class="notice error">${esc(x.message)}</div>`}finally{btn.disabled=false}};$('#redeem').onclick=async()=>{if(!current||!confirm('Redeem this voucher now?'))return;try{await api('/api/redeem-voucher',{method:'POST',body:JSON.stringify({code:current.code,action:'redeem'})});alert('Voucher redeemed successfully.');current=null;$('#redeem').disabled=true;$('#voucher-result').innerHTML=''}catch(x){alert(x.message)}}}async function users(){if(state.user.role!=='superuser')return $('#content').innerHTML='<div class="notice error">Superuser access required.</div>';const d=await api('/api/dashboard-users');$('#content').innerHTML=`<div class="grid-two"><div class="panel form-panel"><span class="section-kicker">Administration</span><h2>Add staff member</h2><p class="form-intro">Create an individual account so every operational action has an owner.</p><form id="user-form"><label>Full name<input name="name" placeholder="e.g. Jane Wanjiku" required></label><label>Email address<input name="email" type="email" placeholder="staff@ricaspa.co.ke" required></label><label>Temporary password<input name="password" type="password" minlength="8" placeholder="At least 8 characters" required></label><label>Access level<select name="role"><option value="employee">Staff</option><option value="superuser">Superuser</option></select></label><button>Create staff account</button></form></div><div class="panel"><div class="panel-head"><div><span class="section-kicker">Access</span><h2>People with access</h2></div></div><div class="user-list">${(d.users||[]).map(u=>`<div class="user-row"><div class="avatar">${esc((u.name||'?').slice(0,1).toUpperCase())}</div><div class="user-main"><b>${esc(u.name)}</b><small>${esc(u.email)}</small></div><span class="pill">${esc(u.role)}</span><select onchange="userStatus('${esc(u.id)}',this.value)"><option value="${esc(u.status)}">${esc(u.status)}</option><option value="active">Active</option><option value="disabled">Disabled</option><option value="pending">Pending</option></select></div>`).join('')||'<div class="empty">No staff accounts yet.</div>'}</div></div></div>`;$('#user-form').onsubmit=async e=>{e.preventDefault();const o=Object.fromEntries(new FormData(e.target));try{await api('/api/dashboard-users',{method:'POST',body:JSON.stringify(o)});await users()}catch(x){alert(x.message)}}}window.userStatus=async(id,status)=>{try{await api('/api/dashboard-users',{method:'PATCH',body:JSON.stringify({id,status})});await users()}catch(e){alert(e.message)}};async function audit(){const d=await api('/api/dashboard-audit');$('#content').innerHTML=`<div class="panel"><div class="panel-head"><div><span class="section-kicker">Accountability</span><h2>Activity log</h2></div><button class="secondary" onclick="render()">Refresh</button></div><div class="table-wrap"><table><thead><tr><th>When</th><th>Staff member</th><th>Action</th><th>Entity</th></tr></thead><tbody>${(d.events||[]).map(a=>`<tr><td>${esc(a.created_at)}</td><td><strong>${esc(a.user_name||'System')}</strong></td><td>${esc(a.action)}</td><td>${esc(a.entity_type||'')} ${esc(a.entity_id||'')}</td></tr>`).join('')||'<tr><td colspan="4" class="empty">No activity recorded yet.</td></tr>'}</tbody></table></div></div>`}(async()=>{try{const d=await api('/api/dashboard-me');state.user=d.user;shell();$('#who').textContent=state.user.name;$('#role').textContent=state.user.role==='superuser'?'Superuser':'Staff';$('#profile-badge').textContent=(state.user.name||'R').slice(0,1).toUpperCase();if(state.user.role!=='superuser')document.querySelectorAll('.superuser-only').forEach(x=>x.remove());await render()}catch(e){location.href='/dashboard/login.html'}})();