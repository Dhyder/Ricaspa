import React from 'react';
import { createRoot } from 'react-dom/client';
import { LayoutDashboard, CalendarDays, MessageSquare, Ticket, ShoppingBag, Users, UserCircle, Activity, LogOut } from 'lucide-react';
import './styles.css';

const nav=[['Overview',LayoutDashboard],['Bookings',CalendarDays],['Messages',MessageSquare],['Vouchers',Ticket],['Orders',ShoppingBag],['Customers',Users],['Staff & Users',UserCircle],['Activity',Activity]];
function App(){const [section,setSection]=React.useState('Overview');return <div className="rica-shell"><aside><div className="logo"><img src="/assets/img/logo.png"/><span>Rica Spa<small>Admin workspace</small></span></div><div className="label">Workspace</div>{nav.slice(0,6).map(([n,I])=><button className={section===n?'active':''} onClick={()=>setSection(n)}><I size={17}/>{n}</button>)}<div className="label">Administration</div>{nav.slice(6).map(([n,I])=><button className={section===n?'active':''} onClick={()=>setSection(n)}><I size={17}/>{n}</button>)}<div className="account"><div className="avatar">R</div><div><b>Rica Admin</b><small>Superuser</small></div><LogOut size={16}/></div></aside><main><header><div><span>RICA SPA / ADMIN</span><h1>{section}</h1></div><UserCircle size={28}/></header><div className="page"><div className="notice"><div><small>RICA SPA OPERATIONS</small><h2>Good to see you.</h2><p>Your operational workspace is being rebuilt with the original template's components.</p></div><div className="mark">R</div></div><div className="cards">{[['Bookings','—'],['Pending','—'],['Orders','—'],['Messages','—']].map(([a,b])=><div className="card"><small>{a}</small><strong>{b}</strong><span>Live D1 data</span></div>)}</div><div className="panel"><div className="panel-title"><div><small>WORKSPACE</small><h2>{section}</h2></div><button>Refresh</button></div><div className="empty"><h3>{section} component foundation</h3><p>The React shell is isolated and ready for the real D1/API data and template components.</p></div></div></div></main></div>}
createRoot(document.getElementById('root')).render(<App/>);
