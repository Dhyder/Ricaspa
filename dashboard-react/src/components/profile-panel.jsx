import React from 'react';
export default function ProfilePanel({user,onSaved}){const [edit,setEdit]=React.useState(false);const [form,setForm]=React.useState({name:user?.name||'Rica Admin',email:user?.email||'',phone:user?.phone||''});const [saving,setSaving]=React.useState(false);const [error,setError]=React.useState('');const save=async e=>{e.preventDefault();setSaving(true);setError('');try{const r=await fetch('/api/dashboard-users',{method:'PATCH',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await r.json();if(!r.ok)throw Error(d.error||'Could not save profile');setEdit(false);onSaved?.(d.user||form)}catch(x){setError(x.message)}finally{setSaving(false)}};return <section className="profile-panel"><div className="profile-hero"><div className="profile-avatar">{(form.name||'R').slice(0,1).toUpperCase()}</div><div><small>MY PROFILE</small><h2>{form.name}</h2><p>{user?.role||'Dashboard user'}</p></div><button onClick={()=>setEdit(!edit)}>{edit?'Cancel':'Edit profile'}</button></div>{edit?<form onSubmit={save} className="profile-form">{error&&<div className="error">{error}</div>}<label>Full name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/></label><label>Email<input value={form.email} onChange={e=>setForm({...form,email:e.target.value})} type="email" required/></label><label>Phone<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><button className="primary" disabled={saving}>{saving?'Saving…':'Save changes'}</button></form>:<div className="profile-details"><div><span>Name</span><b>{form.name}</b></div><div><span>Email</span><b>{form.email||'—'}</b></div><div><span>Role</span><b>{user?.role||'—'}</b></div></div>}</section>}
