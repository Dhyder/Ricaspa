import React from 'react';
export default function TemplatePagination({page,setPage,pageSize,setPageSize,total}){const pages=Math.max(1,Math.ceil(total/pageSize));const p=Math.min(page,pages);const from=total?((p-1)*pageSize)+1:0;const to=Math.min(p*pageSize,total);const go=n=>setPage(Math.max(1,Math.min(pages,n)));return <div className="template-pagination"><div className="rows-control"><span>Rows per page</span><select value={pageSize} onChange={e=>{setPageSize(Number(e.target.value));setPage(1)}}><option value={10}>10</option><option value={20}>20</option><option value={30}>30</option><option value={50}>50</option></select></div><span className="range">{from}–{to} of {total}</span><button disabled={p<=1} onClick={()=>go(1)} aria-label="First page">«</button><button disabled={p<=1} onClick={()=>go(p-1)} aria-label="Previous page">‹</button><span className="page-number">{p} / {pages}</span><button disabled={p>=pages} onClick={()=>go(p+1)} aria-label="Next page">›</button><button disabled={p>=pages} onClick={()=>go(pages)} aria-label="Last page">»</button></div>}
